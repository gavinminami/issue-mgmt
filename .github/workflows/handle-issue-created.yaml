name: Add runbook in comment
on:
  issues:
    types:
      - reopened
      - opened
jobs:
  add_runbook:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - run: |
          pwd
          ls -al
      - uses: actions/github-script@v6
        with:
          script: |
            console.log(context.issue);
            const _issue = await github.rest.issues.get({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
            });
            console.log(_issue);
            console.log(JSON.stringify({labels:_issue.data.labels}));
            console.log(_issue.data.body);

            const { parseData } = require(`./dist/index.js`);
            console.log(parseData);
            console.log(parseData(_issue.data.body));

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `👋 Thanks for reporting! ${_issue.data.title}`
            });
